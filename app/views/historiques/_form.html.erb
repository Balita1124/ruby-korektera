<%= form_for(@historique) do |f| %>
  <% if @historique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historique.errors.count, "error") %> prohibited this historique from being saved:</h2>

      <ul>
      <% @historique.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :action %><br>
    <%= f.text_field :action %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
